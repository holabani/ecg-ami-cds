# CardioSense Backend — Python Dependencies
# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.10.0

# Scientific computing (ECG signal processing)
numpy>=1.26.0
scipy>=1.11.0               # Butterworth bandpass + notch filters (ECGPreprocessor)
neurokit2>=0.2.7            # Pan-Tompkins R-peak detection
PyWavelets>=1.5.0           # DWT feature extraction (ECGFeatureExtractor)
wfdb>=4.1.2                 # MIT/PhysioBank WFDB read for waveform upload

# Deep learning — CNN-BiLSTM inference (NumPy fallback only if torch missing)
torch>=2.1.0

# Explainability (optional — requires torch)
# captum>=0.7.0             # Grad-CAM via LayerGradCam
# shap>=0.44.0              # SHAP feature attribution

# Monitoring & observability
prometheus-client>=0.19.0

# File upload support
python-multipart>=0.0.6

# Auth & persistent history (FYP demo)
sqlalchemy>=2.0.0
bcrypt>=4.1.2
python-jose[cryptography]>=3.3.0
email-validator>=2.1.0

# Testing
pytest>=8.0.0
httpx>=0.27.0               # Required by FastAPI TestClient
